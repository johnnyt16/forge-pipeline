{
  "name": "@forge/admin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port 3000",
    "build": "prisma generate --schema ../../packages/core/prisma/schema.prisma && prisma db push --schema ../../packages/core/prisma/schema.prisma --skip-generate && next build",
    "start": "next start --port ${PORT:-3000}"
  },
  "dependencies": {
    "next": "^14.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@forge/core": "*",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "typescript": "^5.4.0"
  }
}
